Aiutami a creare un app go che comprime le immagini. Sono su macos voglio trascinare un immagine o un gruppo di immagini o una cartella con immagini e l'app  le comprime e le salva nella directory corrente 

Ok adesso le funzioni base ci sono. 
Voglio realizzare un app macos e  windows che comprime le immagini via CLI in golang.
Le immagini possono essere coricate dall'utebtevia_
- immagine singola
- più immagini
- cartetta di immagini

Vorrei la CLI più verbosa ossia:
le immagini prima che l'utente dia l'invio devono essere ordinate in lista con lolonne (un immagine per riga) con accanto la dimensione originale e la dimensione compressa e la percentuale.
L'ultima riga deve avere il totale risparmiato (in Mb e percetuale) e la richiesta all'utente se vuole procedere.

Lapp deve essere compilata come una app cliccabile con icona sia per macos che per windows (arm e intel).

Valuta se è meglio riscrivere da zero l'applicazione e lascia l'applicazione attuale
